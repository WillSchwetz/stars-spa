name: CI/CD Pipeline

on:
  push:
    branches: ["main"]
    # paths:
    #   - 'app/**'
    #   - 'package.json'
    #   - 'Dockerfile'

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ghcr.io/willschwetz/stars-spa:latest

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Notify Start (ntfy)
        run: |
          curl -d "🚀 Deploying The Stars Car Map..." ntfy.sh/grand-current-co-alerts-2026

      - name: Deploy to Linode via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.LINODE_IP }}
          username: ${{ secrets.LINODE_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /var/www/grand-current

            # Login to pull your private stars-spa image
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

            # Pull new images and restart services
            docker compose pull
            docker compose up -d --remove-orphans

            # Clean up dangling images to save space
            docker image prune -f

      - name: Health Check
        id: healthcheck
        run: |
          # Wait 10 seconds for Nginx to settle
          sleep 10
          # Added -k flag to bypass SSL verification strictly for the container status check
          STATUS=$(curl -k -o /dev/null -s -w "%{http_code}" https://stars.willschwetz.ca)
          if [ "$STATUS" -eq 200 ]; then
            echo "site_status=online" >> $GITHUB_OUTPUT
          else
            echo "site_status=offline" >> $GITHUB_OUTPUT
            exit 1
          fi

      - name: Success Notification (ntfy)
        if: success()
        run: |
          curl \
            -H "Title: Deploy Success " \
            -H "Priority: high" \
            -H "Tags: white_check_mark,chart_with_upwards_trend" \
            -H "Actions: view, Open Analytics, https://stars.willschwetz.ca, clear=true; view, Visit Site, https://stars.willschwetz.ca" \
            -d "The Grand Current is live! Click below to check your traffic." \
            ntfy.sh/grand-current-co-alerts-2026

      - name: Failure Notification (ntfy)
        if: failure()
        run: |
          curl -H "Title: Deploy FAILED" \
               -H "Priority: urgent" \
               -H "Tags: warning,skull" \
               -H "Actions: view, Open Analytics, https://stars.willschwetz.ca, clear=true; view, Visit Site, https://stars.willschwetz.ca" \
               -d "Deployment failed. Check GitHub Actions logs for details." \
               ntfy.sh/grand-current-co-alerts-2026
